# alfboard interview (voice) — 브라우저 음성 인터뷰 런타임 deps.
# 설치: uv pip install -r requirements.txt   (버전 고정: reproducible installs)
#
# 브라우저 경로는 Python 쪽에서 httpx + python-dotenv 만 사용한다.
# 음성 대화 자체는 브라우저의 @elevenlabs/client(JS, esm.sh)가 담당하므로
# Python용 elevenlabs SDK / 로컬 마이크(sounddevice)는 필요 없다.

# signed URL 발급 · 에이전트 생성/갱신 · transcript 조회 (ElevenLabs REST)
httpx==0.28.1
# .env 에서 ELEVENLABS_API_KEY / AGENT_ID 로드
python-dotenv==1.2.2
# httpx SOCKS 지원 — Claude Code 샌드박스가 네트워크를 로컬 SOCKS 프록시로 강제할 때
socksio==1.0.0
